version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: faq-suggestion-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

networks:
  default:
    name: faq-api-network
